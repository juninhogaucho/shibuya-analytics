import { useState } from 'react'
import { useNavigate, Link } from 'react-router-dom'
import { motion } from 'motion/react'
import { 
  TrendingDown, 
  Check, 
  ArrowRight, 
  BarChart2, 
  AlertTriangle
} from 'lucide-react'
import { TestimonialsSection } from '../../components/ui/TestimonialsSection'

// --- Components from homepage2.html ---

const DashboardPreview = ({ variant }: { variant: string }) => {
  return (
    <div className="glass-card rounded-xl p-6 h-full min-h-[200px] flex flex-col relative overflow-hidden group hover:bg-white/[0.04] transition-colors">
      <div className="flex items-center justify-between mb-4">
        <div className="flex items-center gap-2">
          <div className={`w-2 h-2 rounded-full ${variant === 'discipline-tax' ? 'bg-red-500' : 'bg-blue-500'}`}></div>
          <span className="text-xs font-medium text-zinc-400 uppercase tracking-wider">
            {variant === 'discipline-tax' ? 'Discipline Tax' : 
             variant === 'bql-score' ? 'BQL Score' :
             variant === 'edge-portfolio' ? 'Edge Portfolio' : 'Alerts'}
          </span>
        </div>
      </div>
      
      {variant === 'discipline-tax' && (
        <div className="mt-auto">
           <div className="text-3xl font-semibold text-white tracking-tight">-€847.20</div>
           <div className="text-xs text-red-400 mt-1 flex items-center gap-1">
              <TrendingDown className="w-3 h-3" />
              12% increase from last month
           </div>
           <div className="mt-4 space-y-2">
              <div className="h-1 w-full bg-zinc-800 rounded-full overflow-hidden">
                  <div className="h-full bg-red-500 w-[70%]"></div>
              </div>
              <div className="flex justify-between text-[10px] text-zinc-500">
                  <span>Revenge</span>
                  <span>€592</span>
              </div>
           </div>
        </div>
      )}

      {variant === 'bql-score' && (
        <div className="flex items-center justify-center h-full">
            <div className="relative w-24 h-24 rounded-full border-4 border-zinc-800 flex items-center justify-center">
                <span className="text-2xl font-bold text-white">72</span>
                <svg className="absolute inset-0 w-full h-full -rotate-90" viewBox="0 0 100 100">
                    <circle cx="50" cy="50" r="46" fill="none" stroke="currentColor" strokeWidth="8" className="text-blue-500 text-opacity-20" />
                    <circle cx="50" cy="50" r="46" fill="none" stroke="currentColor" strokeWidth="8" className="text-blue-500" strokeDasharray="289" strokeDashoffset="80" />
                </svg>
            </div>
        </div>
      )}

      {variant === 'edge-portfolio' && (
        <div className="space-y-3 mt-auto">
            {[
                { name: 'London Breakout', win: '62%', pnl: '+€1,240', color: 'text-green-400' },
                { name: 'Asian Range', win: '34%', pnl: '-€890', color: 'text-red-400' }
            ].map((s, i) => (
                <div key={i} className="flex justify-between items-center text-xs">
                    <span className="text-zinc-300">{s.name}</span>
                    <span className={s.color}>{s.pnl}</span>
                </div>
            ))}
        </div>
      )}

      {variant === 'alerts' && (
        <div className="space-y-3 mt-auto">
            <div className="p-2 rounded bg-red-500/10 border border-red-500/20 flex gap-2">
                <AlertTriangle className="w-4 h-4 text-red-500 flex-shrink-0" />
                <div>
                    <div className="text-[10px] text-red-400 font-bold">TILT DETECTED</div>
                    <div className="text-[10px] text-zinc-400">Stop trading. 3 losses in 15 mins.</div>
                </div>
            </div>
        </div>
      )}
    </div>
  )
}

const AnimatedBeams = () => (
  <div className="absolute inset-0 overflow-hidden pointer-events-none">
      <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-[500px] opacity-30 bg-blue-600/20 blur-[100px] rounded-full mix-blend-screen" />
      <div className="absolute top-[-100px] left-1/4 w-[600px] h-[400px] opacity-20 bg-purple-600/20 blur-[80px] rounded-full mix-blend-screen" />
  </div>
)

export function HomePage() {
  const navigate = useNavigate()
  const [email, setEmail] = useState('')
  const [waitlistSubmitted, setWaitlistSubmitted] = useState(false)
  const [contactForm, setContactForm] = useState({ name: '', email: '', message: '' })
  const [contactSubmitted, setContactSubmitted] = useState(false)
  const [openFaq, setOpenFaq] = useState<number | null>(null)

  // Animation configs
  const fadeUp: any = {
      hidden: { opacity: 0, y: 20 },
      visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: "easeOut" } }
  }

  const staggerContainer: any = {
      hidden: { opacity: 0 },
      visible: { opacity: 1, transition: { staggerChildren: 0.1 } }
  }

  const handleExploreDemo = () => {
    localStorage.setItem('shibuya_api_key', 'shibuya_demo_mode')
    navigate('/dashboard')
  }

  const handleWaitlistSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    const subject = encodeURIComponent('Dashboard Waitlist Signup')
    const body = encodeURIComponent(`New waitlist signup: ${email}\n\nInterested in the €250/mo live dashboard when it launches.`)
    window.open(`mailto:support@shibuya-analytics.com?subject=${subject}&body=${body}`, '_blank')
    setWaitlistSubmitted(true)
    setEmail('')
  }

  const handleContactSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    const subject = encodeURIComponent(`Question from ${contactForm.name}`)
    const body = encodeURIComponent(`Name: ${contactForm.name}\nEmail: ${contactForm.email}\n\n${contactForm.message}`)
    window.location.href = `mailto:support@shibuya-analytics.com?subject=${subject}&body=${body}`
    setContactSubmitted(true)
    setContactForm({ name: '', email: '', message: '' })
  }

  const faqs = [
      {
      q: "How does Shibuya differ from journal apps?",
      a: "Journal apps track what you did. We calculate what it cost you. We quantify your emotions in euros, stripping away the 'luck' factor to show your true behavioral edge."
      },
      {
      q: "Is this only for forex traders?",
      a: "No. We support any market with timestamped trade data: Crypto, Futures, Stocks, Indices. If you can export a CSV, we can analyze your psychology."
      },
      {
      q: "What is the 'Discipline Tax'?",
      a: "It's a proprietary metric that isolates P&L lost specifically to rule violations—revenge trading, oversizing, and tilt—separating them from valid system losses."
      },
      {
      q: "How secure is my trading data?",
      a: "We only process execution data. We don't have access to your broker account funds or withdrawal permissions. Data is encrypted at rest and in transit."
      },
      {
        q: "What if my trading is already profitable?",
        a: "Profitable traders still leave money on the table. We've seen traders making €5k/month who are losing €2k to discipline failures. Imagine keeping that €2k. The report shows you exactly where to tighten up."
      },
      {
        q: "How long until I get my report?",
        a: "Within 72 hours of sending your data. For the €149 Deep Dive, we schedule the first call within a week of delivering your report."
      },
  ]

  return (
    <div className="min-h-screen relative overflow-x-hidden selection:bg-blue-500/30 font-sans bg-[#030304] text-[#e5e5e5]">
        
        {/* Sticky Nav */}
        <nav className="fixed top-0 w-full z-50 border-b border-white/5 bg-[#030304]/80 backdrop-blur-md">
            <div className="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
                <div className="flex items-center gap-2">
                    <div className="w-6 h-6 bg-gradient-to-tr from-blue-500 to-indigo-500 rounded flex items-center justify-center text-[10px] font-bold text-white">S</div>
                    <span className="text-sm font-semibold tracking-tight text-white">shibuya</span>
                </div>
                <div className="hidden md:flex items-center gap-8 text-sm font-medium text-zinc-400">
                    <a href="#methodology" className="hover:text-white transition-colors">Methodology</a>
                    <a href="#pricing" className="hover:text-white transition-colors">Pricing</a>
                    <a href="#faq" className="hover:text-white transition-colors">FAQ</a>
                </div>
                <button 
                  onClick={handleExploreDemo}
                  className="text-xs font-medium bg-white text-black px-4 py-2 rounded-full hover:bg-zinc-200 transition-colors"
                >
                  Launch Demo
                </button>
            </div>
        </nav>

        {/* HERO */}
        <section className="relative pt-32 pb-20 md:pt-48 md:pb-32 px-6 border-b border-white/5 bg-grid-pattern overflow-hidden">
            <AnimatedBeams />
            <motion.div 
                className="max-w-4xl mx-auto text-center relative z-10"
                initial="hidden" animate="visible" variants={staggerContainer}
            >
                <motion.div variants={fadeUp} className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-blue-500/20 bg-blue-500/10 text-blue-400 text-xs font-medium mb-8">
                    <span className="relative flex h-2 w-2">
                        <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                        <span className="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
                    </span>
                    Now compatible with MT4, MT5 & cTrader
                </motion.div>
                
                <motion.h1 variants={fadeUp} className="text-5xl md:text-7xl lg:text-8xl font-semibold tracking-tight text-white mb-8 leading-[1.1]">
                    You're not losing to <br />
                    <span className="text-zinc-500">the market.</span>
                </motion.h1>
                
                <motion.p variants={fadeUp} className="text-lg md:text-xl text-zinc-400 max-w-2xl mx-auto mb-10 leading-relaxed">
                    We calculate exactly how much money you're leaving on the table to <span className="text-rose-400 font-medium">revenge trading</span>, <span className="text-amber-400 font-medium">oversizing</span>, and <span className="text-indigo-400 font-medium">hesitation</span>.
                </motion.p>
                
                <motion.div variants={fadeUp} className="flex flex-col sm:flex-row items-center justify-center gap-4">
                    <a href="#pricing" className="w-full sm:w-auto px-8 py-3.5 bg-white text-black text-sm font-semibold rounded-lg hover:bg-zinc-200 transition-all flex items-center justify-center gap-2 group">
                        Get Your Report 
                        <ArrowRight className="w-4 h-4 group-hover:translate-x-0.5 transition-transform" />
                    </a>
                    <button 
                      onClick={handleExploreDemo}
                      className="w-full sm:w-auto px-8 py-3.5 bg-zinc-900 border border-zinc-800 text-white text-sm font-medium rounded-lg hover:bg-zinc-800 transition-all"
                    >
                        View Live Demo
                    </button>
                </motion.div>

                {/* Hero Visual Mock */}
                <motion.div 
                    variants={fadeUp}
                    className="mt-20 -mb-40 md:-mb-64 mx-auto max-w-5xl bg-[#0a0a0b] rounded-t-xl border border-white/10 shadow-2xl overflow-hidden perspective-1000"
                    style={{ transform: 'rotateX(10deg)', opacity: 0.9 }}
                >
                    <div className="h-8 border-b border-white/5 bg-[#0a0a0b] flex items-center px-4 gap-2">
                        <div className="w-3 h-3 rounded-full bg-red-500/20 border border-red-500/50"></div>
                        <div className="w-3 h-3 rounded-full bg-amber-500/20 border border-amber-500/50"></div>
                        <div className="w-3 h-3 rounded-full bg-green-500/20 border border-green-500/50"></div>
                    </div>
                    <div className="p-1 bg-[#0a0a0b]">
                        <img src="https://hoirqrkdgbmvpwutwuwj.supabase.co/storage/v1/object/public/assets/assets/917d6f93-fb36-439a-8c48-884b67b35381_1600w.jpg" alt="Dashboard" className="w-full opacity-60 hover:opacity-100 transition-opacity duration-700" />
                    </div>
                    <div className="absolute inset-0 bg-gradient-to-t from-[#030304] via-transparent to-transparent h-full w-full pointer-events-none"></div>
                </motion.div>
            </motion.div>
        </section>

        {/* STATS BENTO */}
        <section className="py-24 md:py-32 relative z-10 bg-[#030304]">
            <div className="max-w-7xl mx-auto px-6">
                <div className="mb-16">
                    <h2 className="text-3xl font-semibold text-white tracking-tight mb-4">The Cost of Emotion</h2>
                    <p className="text-zinc-400">These aren't estimates. These are averages from 12,000+ analyzed trade journals.</p>
                </div>
                <div className="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-4 auto-rows-[180px]">
                    <div className="col-span-1 md:col-span-2 row-span-2 glass-card rounded-2xl p-8 flex flex-col justify-between bg-gradient-to-br from-indigo-500/10 to-transparent">
                        <div>
                            <div className="flex items-center gap-2 mb-2">
                                <span className="px-2 py-1 rounded bg-indigo-500/20 text-indigo-300 text-[10px] font-bold uppercase tracking-wider">Discipline Tax</span>
                            </div>
                            <h3 className="text-5xl md:text-7xl font-medium text-white tracking-tighter mt-4">€3,418</h3>
                        </div>
                        <p className="text-zinc-400 max-w-sm text-sm leading-relaxed">The average amount a profitable trader gives back to the market every month purely due to behavioral errors.</p>
                    </div>
                    <div className="glass-card rounded-2xl p-6 flex flex-col justify-between">
                        <TrendingDown className="text-rose-400 w-6 h-6" />
                        <div>
                            <div className="text-2xl font-semibold text-white mb-1">€847</div>
                            <div className="text-xs text-zinc-500">Avg. monthly loss to revenge trading</div>
                        </div>
                    </div>
                    <div className="glass-card rounded-2xl p-6 flex flex-col justify-between">
                        <BarChart2 className="text-amber-400 w-6 h-6" />
                        <div>
                            <div className="text-2xl font-semibold text-white mb-1">3.2x</div>
                            <div className="text-xs text-zinc-500">Size on losing trades vs winning trades</div>
                        </div>
                    </div>
                    <div className="col-span-1 md:col-span-2 glass-card rounded-2xl p-8 flex items-center justify-between">
                        <div>
                            <div className="text-lg text-white font-medium mb-2">The Setup Trap</div>
                            <p className="text-sm text-zinc-400 max-w-xs">47% of traders continue taking specific setups that haven't been profitable for over 90 days.</p>
                        </div>
                        <div className="h-16 w-16 rounded-full border-4 border-zinc-800 border-t-rose-500 flex items-center justify-center bg-zinc-900">
                            <span className="text-sm font-bold text-rose-500">47%</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        {/* METHODOLOGY / EQUATION */}
        <section id="methodology" className="py-24 border-y border-white/5 bg-zinc-900/30">
            <div className="max-w-4xl mx-auto px-6 text-center">
                <span className="text-[10px] font-bold tracking-[0.2em] text-blue-400 uppercase mb-6 block">The Methodology</span>
                
                <motion.div 
                    initial={{ opacity: 0, scale: 0.95 }}
                    whileInView={{ opacity: 1, scale: 1 }}
                    viewport={{ once: true }}
                    className="inline-block p-8 md:p-12 rounded-3xl bg-[#050505] border border-white/10 mb-12 shadow-2xl"
                >
                    <div className="text-3xl md:text-5xl font-mono font-medium tracking-tight">
                        <span className="text-green-400">PnL</span>
                        <span className="text-zinc-600 mx-2">=</span>
                        <span className="text-zinc-400">(</span>
                        <span className="text-blue-400">Edge</span>
                        <span className="text-zinc-600 mx-2">+</span>
                        <span className="text-purple-400">Luck</span>
                        <span className="text-zinc-400">)</span>
                        <span className="text-zinc-600 mx-2">−</span>
                        <span className="text-rose-500">Behavior</span>
                    </div>
                </motion.div>

                <div className="grid md:grid-cols-3 gap-8 text-left">
                    <div className="space-y-2">
                        <div className="text-purple-400 font-mono text-xs uppercase tracking-wider">Step 1</div>
                        <h3 className="text-white font-medium">Filter out Luck</h3>
                        <p className="text-sm text-zinc-500 leading-relaxed">We use robust regression to separate market noise from your actual decision making.</p>
                    </div>
                    <div className="space-y-2">
                        <div className="text-rose-500 font-mono text-xs uppercase tracking-wider">Step 2</div>
                        <h3 className="text-white font-medium">Isolate Behavior</h3>
                        <p className="text-sm text-zinc-500 leading-relaxed">We identify the specific emotional patterns (tilt, hesitation, greed) costing you money.</p>
                    </div>
                    <div className="space-y-2">
                        <div className="text-blue-400 font-mono text-xs uppercase tracking-wider">Step 3</div>
                        <h3 className="text-white font-medium">Reveal True Edge</h3>
                        <p className="text-sm text-zinc-500 leading-relaxed">What remains is your actual trading ability, stripped of emotional interference.</p>
                    </div>
                </div>
            </div>
        </section>

        {/* PROBLEM - Sound Familiar? (Ported from old TSX) */}
        <section className="py-24 relative bg-[#030304]">
          <div className="max-w-6xl mx-auto px-6">
            <div className="text-center mb-16">
              <span className="text-blue-400 text-xs font-bold mb-4 tracking-[0.2em] uppercase block">Sound familiar?</span>
              <h2 className="text-3xl md:text-4xl font-semibold mb-4 tracking-tight text-white">You know exactly what's killing your account.</h2>
              <p className="text-zinc-400 max-w-xl mx-auto">
                You've read the books. You've backtested. You've journaled. But the same mistakes keep happening.
              </p>
            </div>
            
            <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
              {[
                { icon: '🔥', title: 'The revenge trade', desc: '"Just one more to get back to breakeven." That -€340 becomes -€890 before you close the laptop.' },
                { icon: '📏', title: 'The "I feel good" size-up', desc: 'Your rules say 0.5 lots. But this setup looks perfect, so you go 1.5. It loses. Of course it does.' },
                { icon: '⚡', title: 'The 11-trade Tuesday', desc: '3 trades max was the plan. 11 trades later, you\'re wondering what just happened. Again.' },
                { icon: '📉', title: 'The setup that stopped working', desc: 'That "London FVG" hasn\'t worked in months. But you keep taking it anyway, hoping it comes back.' },
              ].map((problem) => (
                <div 
                  key={problem.title}
                  className="glass-card p-6 rounded-2xl hover:bg-white/[0.04] transition-all"
                >
                  <div className="text-3xl mb-4">{problem.icon}</div>
                  <h3 className="text-lg font-semibold mb-2 text-white">{problem.title}</h3>
                  <p className="text-sm text-zinc-400 leading-relaxed">{problem.desc}</p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* HOW IT WORKS */}
        <section className="py-32 px-6 max-w-7xl mx-auto">
            <div className="flex flex-col md:flex-row md:items-end justify-between mb-20 gap-6">
                <div>
                    <h2 className="text-3xl md:text-4xl font-semibold text-white mb-4 tracking-tight">We put a number on <br />your bad habits.</h2>
                    <p className="text-zinc-400 max-w-md">Send us your trades. We run them through the same behavioral analysis hedge funds use.</p>
                </div>
            </div>

            <div className="grid md:grid-cols-3 gap-8">
                {[
                    { step: "01", title: "Export your trades", desc: "MT4, MT5, cTrader, TradingView, any broker. Just export a CSV — takes 2 minutes." },
                    { step: "02", title: "We analyze everything", desc: "Behavioral patterns, emotional triggers, position sizing violations, overtrading windows. All of it." },
                    { step: "03", title: "Get The Report", desc: "Receive a 15-page PDF deep dive into your psychology and a clear plan to save your capital." }
                ].map((item, idx) => (
                    <div key={idx} className="group">
                        <div className="text-6xl font-bold text-zinc-800 mb-6 group-hover:text-zinc-700 transition-colors">{item.step}</div>
                        <h3 className="text-xl font-medium text-white mb-3">{item.title}</h3>
                        <p className="text-sm text-zinc-400 leading-relaxed">{item.desc}</p>
                    </div>
                ))}
            </div>
        </section>

        {/* BENEFITS - What you'll learn (Ported from old TSX) */}
        <section className="py-24 relative bg-zinc-900/30 border-y border-white/5">
          <div className="max-w-6xl mx-auto px-6">
            <div className="text-center mb-16">
              <span className="text-blue-400 text-xs font-bold mb-4 tracking-[0.2em] uppercase block">What you'll learn</span>
              <h2 className="text-3xl md:text-4xl font-semibold tracking-tight text-white">The report that tells you what your journal can't</h2>
            </div>
            
            <div className="grid sm:grid-cols-2 gap-6">
              {[
                { icon: '💰', title: 'Your Discipline Tax', desc: 'The exact euro amount you lost to emotional mistakes last month. Broken down by type: revenge trades, oversizing, overtrading. No vague percentages — real money.' },
                { icon: '📊', title: 'Your Edge Portfolio', desc: 'Which of your setups are actually profitable (keep trading), which are break-even (review), and which are quietly bleeding you dry (stop immediately).' },
                { icon: '🩺', title: 'Your Trading Prescription', desc: 'Not "manage your emotions better." Real rules: "Don\'t trade GBP after a loss." "Max 3 trades before noon." "Never size up on Fridays." Specific to YOU.' },
                { icon: '⏰', title: 'Your Danger Zones', desc: 'The exact times, days, and conditions when you make your worst decisions. Once you see the pattern, you can\'t unsee it.' },
              ].map((benefit) => (
                <div 
                  key={benefit.title}
                  className="glass-card p-8 rounded-2xl hover:border-blue-500/30 transition-all"
                >
                  <div className="text-3xl mb-4">{benefit.icon}</div>
                  <h3 className="text-xl font-semibold mb-3 text-white">{benefit.title}</h3>
                  <p className="text-zinc-400 leading-relaxed">{benefit.desc}</p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* DASHBOARD PREVIEW GRID */}
        <section className="py-32 bg-[#050505] border-y border-white/5">
            <div className="max-w-7xl mx-auto px-6">
                <div className="text-center mb-16">
                    <span className="text-blue-400 text-xs font-bold mb-4 tracking-[0.2em] uppercase block">Inside The Platform</span>
                    <h2 className="text-3xl md:text-4xl font-semibold text-white mb-4 tracking-tight">See Exactly What You're Getting</h2>
                    <p className="text-zinc-400 max-w-2xl mx-auto">Real metrics from our demo dashboard. This is what your analysis looks like.</p>
                </div>
                
                <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6 h-[400px] md:h-[300px]">
                    <DashboardPreview variant="discipline-tax" />
                    <DashboardPreview variant="bql-score" />
                    <DashboardPreview variant="edge-portfolio" />
                    <DashboardPreview variant="alerts" />
                </div>
            </div>
        </section>

        {/* DATA COMPARISON - Before/After (Ported from old TSX) */}
        <section className="py-24 relative bg-[#030304]">
          <div className="max-w-5xl mx-auto px-6">
            <div className="text-center mb-16">
              <span className="text-blue-400 text-xs font-bold mb-4 tracking-[0.2em] uppercase block">Results</span>
              <h2 className="text-3xl md:text-4xl font-semibold mb-4 tracking-tight text-white">Data doesn't lie.</h2>
              <p className="text-zinc-400">What happens when you finally see the cost of your emotions.</p>
            </div>
            
            <div className="grid md:grid-cols-2 gap-8 relative">
              {/* VS Divider */}
              <div className="hidden md:flex absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 z-10">
                <span className="bg-[#030304] text-zinc-500 font-bold px-4 py-2 rounded-full border border-white/10">VS</span>
              </div>
              
              {/* Before Card */}
              <div className="glass-card p-8 rounded-2xl border-red-500/20 bg-red-500/5">
                <div className="flex items-center gap-2 mb-6">
                  <span className="px-3 py-1 rounded-full bg-red-500/20 text-red-500 text-xs font-bold">Before Shibuya</span>
                  <span className="text-2xl">📉</span>
                </div>
                <div className="space-y-4 mb-6">
                  <div>
                    <span className="text-sm text-zinc-400">Sortino Ratio</span>
                    <div className="text-3xl font-bold text-white">0.38</div>
                  </div>
                  <div>
                    <span className="text-sm text-zinc-400">Monthly P&L</span>
                    <div className="text-3xl font-bold text-white">+€1,200</div>
                  </div>
                </div>
                <div className="p-4 rounded-xl bg-red-500/10 border border-red-500/20">
                  <span className="text-sm text-zinc-400">Discipline Tax</span>
                  <div className="text-2xl font-bold text-red-500">−€3,418</div>
                  <span className="text-xs text-zinc-400">Lost to emotions</span>
                </div>
              </div>

              {/* After Card */}
              <div className="glass-card p-8 rounded-2xl border-green-500/20 bg-green-500/5">
                <div className="flex items-center gap-2 mb-6">
                  <span className="px-3 py-1 rounded-full bg-green-500/20 text-green-400 text-xs font-bold">After 3 Months</span>
                  <span className="text-2xl">📈</span>
                </div>
                <div className="space-y-4 mb-6">
                  <div>
                    <span className="text-sm text-zinc-400">Sortino Ratio</span>
                    <div className="text-3xl font-bold text-white">2.84 <span className="text-sm text-green-500">↑ 647%</span></div>
                  </div>
                  <div>
                    <span className="text-sm text-zinc-400">Monthly P&L</span>
                    <div className="text-3xl font-bold text-white">+€4,100 <span className="text-sm text-green-500">↑ 242%</span></div>
                  </div>
                </div>
                <div className="p-4 rounded-xl bg-green-500/10 border border-green-500/20">
                  <span className="text-sm text-zinc-400">Discipline Tax</span>
                  <div className="text-2xl font-bold text-green-500">−€890</div>
                  <span className="text-xs text-green-500">↓ €2,528 saved/month</span>
                </div>
              </div>
            </div>
          </div>
        </section>

        {/* PRICING */}
        <section id="pricing" className="py-32 relative">
            <div className="max-w-5xl mx-auto px-6">
                <h2 className="text-3xl md:text-4xl font-semibold text-center text-white mb-4 tracking-tight">One-time Investment</h2>
                <p className="text-center text-zinc-400 mb-16">Most traders lose more than this in a single bad afternoon.</p>

                <div className="grid md:grid-cols-2 gap-8 items-start">
                    <div className="glass-card p-8 rounded-2xl">
                        <div className="mb-6">
                            <h3 className="text-xl font-medium text-white mb-2">The Reality Check</h3>
                            <div className="flex items-baseline gap-2">
                                <span className="text-4xl font-bold text-white">€99</span>
                                <span className="text-sm text-zinc-500">one-time</span>
                            </div>
                        </div>
                        <ul className="space-y-4 mb-8">
                            {['Discipline Tax Calculation', 'Behavioral Pattern Analysis', 'Edge Portfolio Breakdown', '72-Hour Turnaround'].map((item, i) => (
                                <li key={i} className="flex items-center gap-3 text-sm text-zinc-300">
                                    <Check className="w-4 h-4 text-zinc-500" />
                                    {item}
                                </li>
                            ))}
                        </ul>
                        <Link to="/checkout/steve" className="block w-full py-3 text-center bg-white/5 hover:bg-white/10 border border-white/10 text-white rounded-lg transition-colors text-sm font-medium">
                            Get The Report
                        </Link>
                    </div>

                    <div className="glass-card p-8 rounded-2xl border-blue-500/30 relative overflow-hidden">
                        <div className="absolute top-0 right-0 bg-blue-500 text-white text-[10px] font-bold px-3 py-1 uppercase tracking-wider">Recommended</div>
                        <div className="mb-6">
                            <h3 className="text-xl font-medium text-white mb-2">The Deep Dive</h3>
                            <div className="flex items-baseline gap-2">
                                <span className="text-4xl font-bold text-white">€149</span>
                                <span className="text-sm text-zinc-500">one-time</span>
                            </div>
                        </div>
                        <ul className="space-y-4 mb-8">
                            {['Everything in Reality Check', '2x 30min Strategy Calls', 'Custom Trading Plan', '30-Day Follow-up Review'].map((item, i) => (
                                <li key={i} className="flex items-center gap-3 text-sm text-zinc-300">
                                    <Check className="w-4 h-4 text-blue-400" />
                                    {item}
                                </li>
                            ))}
                        </ul>
                        <Link to="/checkout/steve-plus" className="block w-full py-3 text-center bg-blue-600 hover:bg-blue-500 text-white rounded-lg transition-colors text-sm font-medium">
                            Get The Deep Dive
                        </Link>
                    </div>
                </div>
            </div>
        </section>

        {/* COMING SOON (Ported from old TSX) */}
        <section className="py-24 relative bg-zinc-900/30 border-y border-white/5">
          <div className="max-w-6xl mx-auto px-6">
            <div className="text-center mb-16">
              <span className="text-blue-400 text-xs font-bold mb-4 tracking-[0.2em] uppercase block">Coming Q1 2026</span>
              <h2 className="text-3xl md:text-4xl font-semibold mb-4 tracking-tight text-white">Real-time tracking is on the way</h2>
              <p className="text-zinc-400 max-w-xl mx-auto">
                Launching as a €250/mo subscription for traders who want ongoing accountability, not just a one-time diagnosis.
              </p>
            </div>
            
            <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
              {[
                { icon: '🔄', title: 'Real-time sync', desc: 'Connect your broker. Trades auto-import. No more CSV uploads.' },
                { icon: '🚨', title: 'Live slump detection', desc: "We'll alert you when you're entering tilt BEFORE you blow up." },
                { icon: '📅', title: 'Weekly coaching alerts', desc: 'Sunday evening prep. Friday review. Stay accountable without hiring a coach.' },
                { icon: '📊', title: 'Live dashboard', desc: 'Your discipline tax updating in real-time. Your edge portfolio always current.' },
              ].map((feature) => (
                <div 
                  key={feature.title}
                  className="glass-card p-6 rounded-2xl text-center"
                >
                  <div className="text-3xl mb-4">{feature.icon}</div>
                  <h3 className="text-lg font-semibold mb-2 text-white">{feature.title}</h3>
                  <p className="text-sm text-zinc-400">{feature.desc}</p>
                </div>
              ))}
            </div>
            
            <div className="mt-16 max-w-md mx-auto text-center">
              {waitlistSubmitted ? (
                <div className="p-6 rounded-2xl bg-green-500/10 border border-green-500/20">
                  <span className="text-green-500 text-2xl mb-2 block">✓</span>
                  <p className="text-zinc-400">You're on the list. We'll be in touch when the dashboard launches.</p>
                </div>
              ) : (
                <form onSubmit={handleWaitlistSubmit} className="flex gap-3">
                  <input
                    type="email"
                    placeholder="Your email"
                    value={email}
                    onChange={(e) => setEmail(e.target.value)}
                    required
                    className="flex-1 px-4 py-3 rounded-full bg-white/5 border border-white/10 text-white placeholder-zinc-500 focus:outline-none focus:border-blue-500"
                  />
                  <button 
                    type="submit" 
                    className="px-6 py-3 bg-blue-600 hover:bg-blue-500 text-white rounded-full font-medium transition-colors"
                  >
                    Join Waitlist
                  </button>
                </form>
              )}
              <p className="text-xs text-zinc-500 mt-4">No spam. Just one email when we launch.</p>
            </div>
          </div>
        </section>

        {/* TESTIMONIALS (Ported from old TSX) */}
        <div className="py-24 bg-[#030304]">
            <TestimonialsSection />
        </div>

        {/* FAQ */}
        <section id="faq" className="py-24 border-t border-white/5 bg-[#030304]">
            <div className="max-w-3xl mx-auto px-6">
                <h2 className="text-2xl font-semibold text-white mb-10 tracking-tight">Common Questions</h2>
                <div className="space-y-2">
                    {faqs.map((faq, i) => (
                        <div key={i} className="border border-white/5 rounded-lg overflow-hidden">
                            <button 
                                onClick={() => setOpenFaq(openFaq === i ? null : i)}
                                className="w-full flex items-center justify-between p-4 text-left hover:bg-white/5 transition-colors"
                            >
                                <span className="text-sm font-medium text-zinc-300">{faq.q}</span>
                                <span className="text-zinc-500">{openFaq === i ? '−' : '+'}</span>
                            </button>
                            {openFaq === i && (
                                <div className="p-4 pt-0 text-sm text-zinc-500 leading-relaxed">
                                    {faq.a}
                                </div>
                            )}
                        </div>
                    ))}
                </div>
            </div>
        </section>

        {/* CONTACT FORM (Ported from old TSX) */}
        <section className="py-24 relative bg-zinc-900/30 border-t border-white/5">
          <div className="max-w-2xl mx-auto px-6">
            <div className="text-center mb-12">
              <span className="text-blue-400 text-xs font-bold mb-4 tracking-[0.2em] uppercase block">Get in touch</span>
              <h2 className="text-3xl md:text-4xl font-semibold mb-4 tracking-tight text-white">Questions? Ideas? Just want to chat?</h2>
              <p className="text-zinc-400">
                We're building this for traders like you. Your feedback shapes what we build next.
              </p>
            </div>
            
            <div>
              {contactSubmitted ? (
                <div className="p-8 rounded-2xl bg-green-500/10 border border-green-500/20 text-center">
                  <span className="text-green-500 text-3xl mb-4 block">✓</span>
                  <p className="text-zinc-400">Message received. We'll get back to you within 24 hours.</p>
                </div>
              ) : (
                <form onSubmit={handleContactSubmit} className="space-y-4">
                  <div className="grid sm:grid-cols-2 gap-4">
                    <input
                      type="text"
                      placeholder="Your name"
                      value={contactForm.name}
                      onChange={(e) => setContactForm({ ...contactForm, name: e.target.value })}
                      required
                      className="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-white placeholder-zinc-500 focus:outline-none focus:border-blue-500"
                    />
                    <input
                      type="email"
                      placeholder="Your email"
                      value={contactForm.email}
                      onChange={(e) => setContactForm({ ...contactForm, email: e.target.value })}
                      required
                      className="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-white placeholder-zinc-500 focus:outline-none focus:border-blue-500"
                    />
                  </div>
                  <textarea
                    placeholder="Your message, question, or suggestion..."
                    value={contactForm.message}
                    onChange={(e) => setContactForm({ ...contactForm, message: e.target.value })}
                    required
                    rows={4}
                    className="w-full px-4 py-3 rounded-xl bg-white/5 border border-white/10 text-white placeholder-zinc-500 focus:outline-none focus:border-blue-500 resize-none"
                  />
                  <button 
                    type="submit" 
                    className="w-full py-3 bg-blue-600 hover:bg-blue-500 text-white rounded-xl font-medium transition-colors"
                  >
                    Send Message
                  </button>
                  <p className="text-xs text-zinc-500 text-center">Opens your email client. We reply within 24 hours.</p>
                </form>
              )}
            </div>
          </div>
        </section>

        {/* CONTACT / FOOTER */}
        <footer className="py-20 border-t border-white/5 bg-[#010101]">
            <div className="max-w-6xl mx-auto px-6">
                <div className="grid md:grid-cols-2 gap-16">
                    <div>
                        <div className="flex items-center gap-2 mb-6">
                            <div className="w-6 h-6 bg-gradient-to-tr from-blue-500 to-indigo-500 rounded flex items-center justify-center text-[10px] font-bold text-white">S</div>
                            <span className="text-sm font-semibold tracking-tight text-white">shibuya</span>
                        </div>
                        <p className="text-zinc-500 text-sm leading-relaxed max-w-xs">
                            We don't teach you how to trade. We show you what your trading is costing you.
                        </p>
                    </div>
                    <div className="grid grid-cols-2 gap-8">
                        <div>
                            <h4 className="text-white font-medium mb-4 text-sm">Product</h4>
                            <ul className="space-y-2 text-sm text-zinc-500">
                                <li><a href="#methodology" className="hover:text-white transition-colors">Methodology</a></li>
                                <li><a href="#pricing" className="hover:text-white transition-colors">Pricing</a></li>
                                <li><a href="#faq" className="hover:text-white transition-colors">FAQ</a></li>
                            </ul>
                        </div>
                        <div>
                            <h4 className="text-white font-medium mb-4 text-sm">Legal</h4>
                            <ul className="space-y-2 text-sm text-zinc-500">
                                <li><a href="#" className="hover:text-white transition-colors">Terms</a></li>
                                <li><a href="#" className="hover:text-white transition-colors">Privacy</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
                <div className="mt-20 pt-8 border-t border-white/5 flex flex-col md:flex-row justify-between items-center text-xs text-zinc-600">
                    <p>&copy; 2025 Shibuya Analytics. All rights reserved.</p>
                    <p>Trading involves risk. Past performance is not indicative of future results.</p>
                </div>
            </div>
        </footer>
    </div>
  )
}
